#include "SDL.h"
#include <stdio.h>
#include <iostream>
#include <string>
using namespace std; 

int main(int argc, char *argv[]) {
	switch (void)
		case SDL_KEYDOWN; {
		// spirte x value plus 1
	}
	return 0;
}